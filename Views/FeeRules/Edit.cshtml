@model ClinicaOdontologica.Models.FeeRule

@{
    ViewData["Title"] = "Editar Regra de Taxa";
}

<h2>Editar Regra de Taxa</h2>

<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            
            <div class="form-group mb-3">
                <label for="paymentMethodId" class="control-label">Forma de Pagamento</label>
                <select id="paymentMethodId" name="paymentMethodId" class="form-control" required onchange="toggleCardBrandField()">
                    <option value="">Selecione...</option>
                    @foreach (var pm in ViewBag.PaymentMethods)
                    {
                        <option value="@pm.Id" selected="@(Model.PaymentMethodId == pm.Id ? "selected" : null)">@pm.Name</option>
                    }
                </select>
            </div>
            
            <div class="form-group mb-3" id="cardBrandField" style="display: @(Model.CardBrandId.HasValue ? "block" : "none");">
                <label for="cardBrandId" class="control-label">Bandeira</label>
                <select id="cardBrandId" name="cardBrandId" class="form-control">
                    <option value="">Selecione...</option>
                    @foreach (var cb in ViewBag.CardBrands)
                    {
                        <option value="@cb.Id" selected="@(Model.CardBrandId == cb.Id ? "selected" : null)">@cb.Name</option>
                    }
                </select>
            </div>
            
            <div class="form-group mb-3" id="installmentsField" style="display: @(Model.Installments.HasValue ? "block" : "none");">
                <label for="installments" class="control-label">Número de Parcelas</label>
                <input type="number" id="installments" name="installments" class="form-control" min="1" max="20" value="@(Model.Installments?.ToString() ?? "")" />
                <small class="text-muted">Deixe em branco para taxa única (sem parcelamento)</small>
            </div>
            
            <div class="form-group mb-3">
                <label for="feePercentageString" class="control-label">Taxa (%)</label>
                <input id="feePercentageString" name="feePercentageString" class="form-control" type="text" placeholder="Ex: 1,53" required value="@Model.FeePercentage.ToString("F2", System.Globalization.CultureInfo.GetCultureInfo("pt-BR"))" />
                <span class="text-danger" id="feePercentageError"></span>
                <small class="text-muted">Digite a taxa em porcentagem (ex: 1,53 para 1,53%)</small>
            </div>
            
            <div class="form-group mb-3">
                <div class="form-check">
                    <input asp-for="Active" class="form-check-input" type="checkbox" />
                    <label asp-for="Active" class="form-check-label"></label>
                </div>
                <span asp-validation-for="Active" class="text-danger"></span>
            </div>
            
            <div class="form-group">
                <input type="submit" value="Salvar" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        function toggleCardBrandField() {
            var paymentMethodId = document.getElementById('paymentMethodId').value;
            var cardBrandField = document.getElementById('cardBrandField');
            var installmentsField = document.getElementById('installmentsField');
            
            var requiresBrand = paymentMethodId !== "";
            
            if (requiresBrand) {
                cardBrandField.style.display = 'block';
                installmentsField.style.display = 'block';
            } else {
                cardBrandField.style.display = 'none';
                installmentsField.style.display = 'none';
            }
        }
    </script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
